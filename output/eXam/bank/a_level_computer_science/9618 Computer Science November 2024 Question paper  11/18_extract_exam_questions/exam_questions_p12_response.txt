[thinking]
**My Analysis of the Exam Question**

Okay, let's break this down systematically. The task is to extract and structure two questions from page 12 of an exam paper into YAML format. The questions are specifically "5b" and "5c". I need to identify the question type, the text content, and the format the output. The output needs to be YAML, including `number`, `type`, `text` (and `options` for MCQs, though these aren't).

First, let's dissect question "5b". It's a `short_answer` question, focused on logic expressions. The prompt provides context: a floodlight (X) and an audio alarm (Y) operate based on specific criteria tied to parameters A to E. The table clarifies these parameters: daylight level (A), front door (B), proximity sensor (C), back door (D), and the security system status (E). Each parameter has a binary value and corresponding condition. I recognize this setup – it's designed to form boolean expressions.

The core of the question lies in the given criteria: the conditions for the floodlight turning on ($X = 1$) and the audio alarm turning on ($Y = 1$). X turns on when the security system is on, daylight is low, and a person is detected. Y turns on when the security system is on and either one or more doors are open or a person is detected. The task is to express these conditions as logic expressions, which will be the content within the "text" field of the YAML. I need to be careful with the answer lines. Initially, they are presented as a fill-in-the-blank format, `X = \hrulefill` and `Y = \hrulefill`. The rule indicates that the filled lines should be `X = expression \hrulefill`.

Now, question "5c". It's a `long_answer` question, requiring a more extended explanation. The core of this question is the definition of the security system; is it a monitoring system or a control system. No specific markers or formats are required. The key is in the understanding of the concepts of monitoring and control. This becomes the `text` field for this question's YAML entry.

Finally, I need to format this all into YAML. I'll make sure to use block scalars (`|`) for the textual content to preserve formatting (especially the latex tables and equations). I'll also ensure that mathematical expressions are wrapped in `$...$` to denote inline math. Crucially, I'll avoid double quotes for free-text fields. Now I can translate the problem in my head into the YAML output.



[/thinking]

questions:
  - number: "5b"
    type: short_answer
    text: |
      The floodlight (\textbf{X}) and audio alarm (\textbf{Y}) operate according to the following criteria:

      \begin{tabular}{|c|c|c|c|}
      \hline
      \textbf{Parameter} & \textbf{Description of parameter} & \textbf{Binary value} & \textbf{Condition} \\ \hline
      \textbf{A} & external daylight level & 1 & Low \\ \cline{3-4}
      & & 0 & High \\ \hline
      \textbf{B} & front door & 1 & Open \\ \cline{3-4}
      & & 0 & Closed \\ \hline
      \textbf{C} & person is within 2 m & 1 & Detected \\ \cline{3-4}
      & & 0 & Not detected \\ \hline
      \textbf{D} & back door & 1 & Open \\ \cline{3-4}
      & & 0 & Closed \\ \hline
      \textbf{E} & security system & 1 & Switched on \\ \cline{3-4}
      & & 0 & Switched off \\ \hline
      \end{tabular}

      The floodlight turns on ($X = 1$) if:
      \begin{itemize}
      \item the security system is switched on \textbf{and}
      \item the external daylight level is low \textbf{and}
      \item a person is detected within 2 m.
      \end{itemize}

      The audio alarm turns on ($Y = 1$) if:
      \begin{itemize}
      \item the security system is switched on \textbf{and}
      \item one \textbf{or} more doors are open, \textbf{or} a person is detected within 2 m.
      \end{itemize}

      Write logic expressions for the security system.

      \textbf{X} = \hrulefill

      \textbf{Y} = \hrulefill
  - number: "5c"
    type: long_answer
    text: |
      Explain whether the security system is an example of a monitoring system or a control system.