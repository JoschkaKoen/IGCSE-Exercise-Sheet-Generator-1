[thinking]
**Transcribing Questions for the User**

Okay, the user wants me to extract and format two questions. Let's see... the first one, question 5d, requires a long answer. It asks me to explain the reasons why the data in the `\texttt{ORDER\_ITEM}` table *cannot* be stored in the `\texttt{ORDER}` table. Ah, right. It seems important to note the proper use of monospace-like font for the table names, `\texttt{ORDER\_ITEM}` and `\texttt{ORDER}`. I confirmed from the image that this is correct.

Now for question 5e. Again, this is a long answer question. This one involves writing SQL. The user wants a SQL script to output the customer ID, customer name, and total cost of unpaid orders. They also specified the output of the total cost needs a title. Got it. Now to write the YAML representation of these questions.



[/thinking]

questions:
  - number: "5d"
    type: long_answer
    text: |
      Explain the reasons why the data in the table \texttt{ORDER\_ITEM} cannot be stored in the table \texttt{ORDER}
  - number: "5e"
    type: long_answer
    text: |
      Write an Structured Query Language (SQL) script to output the customer ID, the customer's name and the total cost of the customer's orders that have not been paid.

      The output of the total cost must have an appropriate title.