[thinking]
**Thought Process: Exam Page Analysis**

Okay, here's how I'm approaching this exam page. First, I need to understand the requested output format: YAML, with `number`, `type`, and `text` keys, and `options` if it's multiple-choice (which it isn't here).

I'm starting by breaking down the content on the page, question by question.

*   **Question 2**: I'm reading the introductory text to build a mental model of the scenario: a 1D array (`HashTable`), collision handling with a `Spare` array, and a key modulo 200 formula. I'll represent this text as it is, using inline code for variable names and the mod operator.

*   **Question 2a**: Here, it's about the record format `NewRecord`, with integer fields `Key`, `Item1`, and `Item2`. I need to capture the pseudocode definition accurately, including the `TYPE` and `ENDTYPE` keywords. The "save as" and "copy and paste" instructions are straightforward text blocks that I'll need to remember, as it seems they are instructions for the students.

*   **Question 2b**: This is where it gets interesting. There's no separate text for "2b." It seems to be a structural element preceding the sub-questions `2bi` and `2bii`. Based on my understanding of exam structure, I'll represent the text for `2b` as empty since no stem text is found and the spec says: "If you cannot find a stub entry's stem on this page, this indicates a stub-generation bug upstream — emit a `|` block scalar containing `STUB ERROR` so QA surfaces it."

*   **Question 2bi**: This is straightforward – declaring the global arrays `HashTable` and `Spare`. I'll focus on the instruction to "write program code" and the save/copy instructions.

*   **Question 2bii**: This question deals with initializing the arrays with empty records. Here, an empty record is defined by using -1 in the integer fields.  I'll capture the instructions to "write program code for `Initialise()`" and the save/copy directions as well.

The final step is to refine the text for each section, making sure to use `\texttt{...}` for inline code and proper math representation for numbers (like -1). I will also adhere to the formatting rules, no double quotes, using block scalar where needed, and making sure to strip any question prefixes like `(a)`, `(b) (i)`, etc. I will not add or remove questions.



[/thinking]

questions:
  - number: "2"
    type: short_answer
    text: |
      A program stores data in a 1D array of records, \texttt{HashTable}. The array has space for 200 records. Each data item is stored in a specific index of the array that is calculated using an algorithm. The index is calculated from the key field using the formula: \texttt{Key MOD 200}

      If two key fields generate the same index, there is a collision. Any records that have a collision are stored in a second array, \texttt{Spare}. This array has space for 100 records. When a collision is detected, the record is stored in the next free space in \texttt{Spare}.
  - number: "2a"
    type: short_answer
    text: |
      The pseudocode record format is:

      \begin{alltt}
      TYPE NewRecord
          DECLARE Key : INTEGER
          DECLARE Item1 : INTEGER
          DECLARE Item2 : INTEGER
      ENDTYPE
      \end{alltt}

      Write program code to declare the record type \texttt{NewRecord}

      If your chosen programming language does not support record formats, a class can be used instead.

      Save your program as \textbf{Question2\_J25}.

      Copy and paste the program code into part \textbf{2(a)} in the evidence document.
  - number: "2b"
    type: short_answer
    text: ''
  - number: "2bi"
    type: short_answer
    text: |
      Write program code to declare the global arrays \texttt{HashTable} and \texttt{Spare}

      Save your program.

      Copy and paste the program code into part \textbf{2(b)(i)} in the evidence document.
  - number: "2bii"
    type: short_answer
    text: |
      An empty record has the integer $-1$ stored in each field.

      The procedure \texttt{Initialise()} stores an empty record in each element in \texttt{HashTable} and \texttt{Spare}

      Write program code for \texttt{Initialise()}

      Save your program.

      Copy and paste the program code into part \textbf{2(b)(ii)} in the evidence document.