[thinking]
**My Analysis of Exam Question Extraction**

Okay, so I've been given a task: to process page 8 of an exam paper and extract specific questions, specifically those labeled "5", "5a", "5b", and "5c". The goal is to format these questions into a YAML structure that perfectly matches the provided stub.

First, I need to understand the structure of the input and the required output. The input is a page from an exam, and I'm assuming it's text-based. I need to be able to identify and isolate the questions. The desired output is a YAML representation, which means I'll need to pay close attention to indentation, key-value pairs, and the use of block scalars for multi-line text.

Now, let's break down the process. I'll need to carefully examine the content of the page, identifying each of the target questions. For example, question "5" is about a LAN with computers, a switch, and a server. For "5a", "5b", and "5c", I need to grab the prompts, making sure to remove the "(a)", "(b)", and "(c)" prefixes. I must also remember that any diagrams or figures within the questions are to be *completely* omitted from the output. The instruction "Never emit `\includegraphics{...}` or any other image-inclusion command" is crystal clear on that point.

Once I've extracted the question text, formatting them into the YAML structure is the next step. Since there are no multiple-choice questions here, I won't need an `options` key. All text fields should use the `|` block scalar to preserve the original formatting, including line breaks and spacing. The YAML must be perfectly valid.



[/thinking]

questions:
  - number: "5"
    type: short_answer
    text: |
      A local area network (LAN) has five computers, one switch and one server.
  - number: "5a"
    type: short_answer
    text: |
      Describe the characteristics of a LAN.
  - number: "5b"
    type: short_answer
    text: |
      Complete the following diagram to show how these devices are connected in a star topology.
  - number: "5c"
    type: long_answer
    text: |
      Describe how packets are transmitted between two hosts using a star topology.