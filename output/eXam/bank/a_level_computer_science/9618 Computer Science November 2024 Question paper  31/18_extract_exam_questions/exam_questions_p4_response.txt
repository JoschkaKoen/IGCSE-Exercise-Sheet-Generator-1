[thinking]
**Analyzing and Transcribing Exam Page 4**

Okay, so I need to transcribe page 4 of this exam paper. First, I see question "4," which is a long-answer question about the TCP/IP protocol stack and how its layers interact. That's straightforward; I can handle that.

Now, question "5" looks a bit trickier. Checking the structure, it appears to be a parent question or, possibly, a stub error. Looking at the supplied stub, I see entries for "5," "5a," and "5b". I'm cross-referencing with the actual page. Question 5 has parts, "5 (a)" and "(b)".

The critical thing is to ensure that every question stem in the stub is visible on the page. I see the page has "5 (a)", which asks about hashing algorithms. So, "5" doesn't have its *own* text on the page, it goes straight to "5 (a)". Based on the instructions, if the page doesn't have text for a stub question, that's a stub-generation issue. I should *not* guess the stem. This indicates a stub error, so I must mark "5" with `STUB ERROR`. It's a key principle of the curation process; QA should surface and handle it.

So, for "5a," I'll transcribe the question about hashing algorithms, and for "5b," the question on collision resolution in hashing. I'll make sure to note that two methods are required, and include the provided placeholders. After double checking question "5b" has slots, the following looks correct.

```yaml
questions:
  - number: "4"
    type: long_answer
    text: |
      The TCP/IP protocol may be viewed as a stack that contains four layers: Application, Transport, Internet, Link.

      Describe how the layers of the TCP/IP protocol stack interact with each other.
  - number: "5"
    type: short_answer
    text: |
      STUB ERROR
  - number: "5a"
    type: long_answer
    text: |
      Explain what is meant by a hashing algorithm in the context of file access.
  - number: "5b"
    type: short_answer
    text: |
      The use of a hashing algorithm can result in the same storage location being identified for more than one record.

      Outline \textbf{two} methods of overcoming this issue.

      1. \hrulefill
      2. \hrulefill
```
And now that's done. I'm ready to move on.



[/thinking]

```yaml
questions:
  - number: "4"
    type: long_answer
    text: |
      The TCP/IP protocol may be viewed as a stack that contains four layers: Application, Transport, Internet, Link.

      Describe how the layers of the TCP/IP protocol stack interact with each other.
  - number: "5"
    type: short_answer
    text: |
      STUB ERROR
  - number: "5a"
    type: long_answer
    text: |
      Explain what is meant by a hashing algorithm in the context of file access.
  - number: "5b"
    type: short_answer
    text: |
      The use of a hashing algorithm can result in the same storage location being identified for more than one record.

      Outline \textbf{two} methods of overcoming this issue.

      1. \hrulefill
      2. \hrulefill
```