[thinking]
**Exam Page 3: YAML Conversion**

Okay, here's the breakdown of what I'm doing with this exam page (page 3, specifically). The goal is to get this information into a clean YAML format for some kind of automated processing or storage, which means extracting question information structured appropriately.

First, I need to understand the structure. The "input" is just this single page with these questions "1c", "1ci", "1cii". The "output" needs to be YAML, with the keys: `number`, `type`, `text`, and possibly `options` if it's a multiple-choice question.

Now, let's analyze the content. I see:

*   **"1c"**: This is essentially a contextual introduction: "The student types a report on a computer in a history lesson." This is straightforward; I can just transcribe it.
*   **"1ci"**:  This is the first *real* question. It looks like a classic fill-in-the-blanks style asking about the characteristics of Unicode. I see, "The computer uses the Unicode character set. Give two characteristics of the Unicode character set. 1. \hrulefill 2. \hrulefill." I'll need to strip the prefix "(i)" and format it properly.
*   **"1cii"**:  This is a table-based question related to ASCII, asking to fill in missing denary, binary and hexadecimal values. I'll need to strip the prefix "(ii)" and capture the table in a suitable format, keeping the structure as LaTeX code. I see, for the last character, I need to represent "ü". I think that I can write it as `\"u` but I should use `\ddot{u}` to be precise, or `$\ddot{\text{u}}$`.

Now for the formatting. "1c" will be transcribed as is, "1ci" needs to include placeholders for the fill-in-the-blanks, and "1cii" requires a LaTeX table.

Let's refine the YAML format. It must follow these criteria:
*   Use block scalars, marked by `|`, for all non-empty text fields, which is good practice.
*   Avoid double quotes.

Finally, I need to double-check the LaTeX code for the table, especially with those characters with diacritics. I confirm that all rows in the table have the same number of columns, so the table formatting is clean. I will be sure to use `\"u` or `\ddot{u}` in the table. Let's make it `\"u`. I am ready to convert the questions to YAML format.



[/thinking]

questions:
  - number: "1c"
    type: short_answer
    text: |
      The student types a report on a computer in a history lesson.
  - number: "1ci"
    type: short_answer
    text: |
      The computer uses the Unicode character set.

      Give \textbf{two} characteristics of the Unicode character set.

      1. \hrulefill
      2. \hrulefill
  - number: "1cii"
    type: short_answer
    text: |
      Some of the school's computers use the extended ASCII character set.

      The table gives some characters from the extended ASCII character set, their denary, 8-bit binary and hexadecimal numbers.

      Complete the table by filling in the missing numbers.

      \begin{tabular}{|c|c|c|c|}
      \hline
      \textbf{Character} & \textbf{Denary} & \textbf{8-bit Binary} & \textbf{Hexadecimal} \\
      \hline
      ! & 33 & & 21 \\
      \hline
      L & & 01001100 & 4C \\
      \hline
      \"u & 252 & 11111100 & \\
      \hline
      \end{tabular}