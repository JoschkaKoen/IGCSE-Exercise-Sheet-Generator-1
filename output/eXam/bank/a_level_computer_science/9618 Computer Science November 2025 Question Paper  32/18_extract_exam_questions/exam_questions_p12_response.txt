[thinking]
**My Thought Process on Parsing Exam Questions**

Okay, so I've got Page 12 of an exam paper. My task is to extract the questions and format them in YAML, matching the specified schema.  First, I need to identify the question numbers, which I see are "12", "12a", "12ai", "12aii", and "12b".  Now, let's analyze the page content to build the YAML structure.

I'm starting with question "12".  It seems to be the main question header, but I need to determine its specific role.  Is it just a context setter, or does it hold a specific question? I notice it says "12 (a)", so "12" might be a general context. I need to understand the hierarchy: "12" is likely the parent, and "12a" is a sub-question. I'll need to figure out where the text actually begins. I'll check the stubs to assign types.

I see that "12" is `short_answer`. This is just the general text for the whole thing.

Now, looking at the layout, "12 (a) A stack has been implemented..." seems like "12a" is the immediate text of the question.  If "12" is meant to be a general title, should I combine the introduction in "12" or "12a"?

It seems "12a" includes the table. This is important to note and properly format later. And, it includes introductory text.

Let's make a new attempt:
*   `12`: Introduce the topic and the table
*   `12a`: The immediate question of (a)
*   `12ai`: First sub-question
*   `12aii`: Second sub-question
*   `12b`: The final question, a `long_answer`

I need to be very precise here. If "12a" exists, then I'll use it.

I'll put the table and introductory details into "12" because it's a general context description.

"12a" will now be: "A stack has been implemented using pseudocode to store a maximum of 100 string items."

I've to populate "12" based on the question text, the table, and the description of the stack implementation.

Next, I go to "12ai". It states: "Complete the pseudocode for the procedure to add a data item onto the stack." The pseudocode is the following:

```latex
\begin{alltt}
PROCEDURE Push(\underline{\hspace{1.5em}})
    IF Top < Max - 1 THEN

        Top <- \underline{\hspace{1.5em}}

        \underline{\hspace{1.5em}} <- NewData
    ELSE

        OUTPUT \underline{\hspace{1.5em}}
    ENDIF
ENDPROCEDURE
\end{alltt}
```

I see those blank spaces ("...") and how the instructions say to format them using `\underline{\hspace{1.5em}}`.

So now, I need to format `12ai` and `12aii` (and the next). Let's do that now.
*   `12ai`: Complete the pseudocode to add an item.
*   `12aii`: Write the pseudocode.
*   `12b`: Explain the reasons for the stack in recursive algorithms.

Finally, I'm going to refine `12` and `12a`. Let's create:
*   `12`: The table and short description of the topic.
*   `12a`: More context and the table.

Now I have a more streamlined structure:
*   `12` = Short header to introduce the stack.
*   `12a` = The table of variables, description, and "A stack..." context.
This is a more concise structure.

Now, I'm ready to write the YAML structure. This is clear and logical.



[/thinking]

questions:
  - number: "12"
    type: short_answer
    text: |
      The following table shows the global variables used to implement a stack.
  - number: "12a"
    type: short_answer
    text: |
      A stack has been implemented using pseudocode to store a maximum of 100 string items using the global variables in the following table:

      \begin{tabular}{|c|c|l|c|}
      \hline
      \textbf{Identifier} & \textbf{Data type} & \textbf{Description} & \textbf{Initialisation value} \\ \hline
      Base & INTEGER & pointer for the bottom of the stack & 0 \\ \hline
      Top & INTEGER & pointer for the top of the stack & -1 \\ \hline
      StackArray & STRING & 1D array to implement the stack & [0:99] \\ \hline
      Max & INTEGER & maximum number of items in the stack & 100 \\ \hline
      \end{tabular}

      The value of \texttt{Top} is incremented each time a data item is added to the stack and decremented each time a data item is removed. If the stack is full, an appropriate error message is output.
  - number: "12ai"
    type: short_answer
    text: |
      Complete the \textbf{pseudocode} for the procedure to add a data item onto the stack.

      \begin{alltt}
      PROCEDURE Push(\underline{\hspace{1.5em}})
          IF Top < Max - 1 THEN

              Top <- \underline{\hspace{1.5em}}

              \underline{\hspace{1.5em}} <- NewData
          ELSE

              OUTPUT \underline{\hspace{1.5em}}
          ENDIF
      ENDPROCEDURE
      \end{alltt}
  - number: "12aii"
    type: short_answer
    text: |
      Write \textbf{pseudocode} to input a new data item and add it to the stack using \texttt{Push()}.
  - number: "12b"
    type: long_answer
    text: |
      Explain the reasons why a stack is used when a recursive algorithm is executed.