[thinking]
**My Analysis of the Exam Page**

Okay, so I've been given page 5 of an exam paper and need to extract and format specific questions into YAML, adhering to a defined schema. I've got a clear task: identify and transcribe questions "3d", "4", "4a", "4b", and "4c". My process will be methodical, ensuring accuracy and proper formatting.

First, I need to extract "3d". I'll strip the leading "(d) " prefix. The resulting text is: "The denary integer $-32$ is stored as a two's complement integer.\n\nCalculate the two's complement integer that would be stored.\n\nShow all your working." This is clearly a calculation-type question.

Next, I move on to "4". After removing the "4 " prefix, I'm left with: "A student uses both system software and application software on their computer." This looks like a straightforward short answer question.

Now, "4a": the prefix "(a) " is removed, leaving "Give \textbf{one} example of system software." Another short answer, but this time it emphasizes the need for a single example.

Moving on to "4b", I see: "Give \textbf{two} examples of application software.\n\n1. \hrulefill\n2. \hrulefill". This is another short answer, and the use of numbered slots with horizontal rules for the answers is critical to note.

Finally, for "4c", I've got: "(c) Describe the difference between system software and application software.", after removing the prefix, I am left with: "Describe the difference between system software and application software." This is also a short answer.

Now, the important part: formatting the YAML. I need to make sure I use block scalars (`|`) for any non-empty text fields. Double quotes need to be avoided for non-empty strings. I'll need to double check the LaTeX formatting, such as the math, the use of `\textbf{one}` and `\textbf{two}` for bolding. Then I will need to consider the rendering rules based on the presence or absence of answer areas.

I need to make sure that the question "3d" will not render with an answer area, and neither will "4a", and "4c", but the numbered slots in "4b" should be preserved to indicate the format for two answers. I am ready to construct the YAML now, taking these factors into account.



[/thinking]

questions:
  - number: "3d"
    type: calculation
    text: |
      The denary integer $-32$ is stored as a two's complement integer.

      Calculate the two's complement integer that would be stored.

      Show all your working.
  - number: "4"
    type: short_answer
    text: |
      A student uses both system software and application software on their computer.
  - number: "4a"
    type: short_answer
    text: |
      Give \textbf{one} example of system software.
  - number: "4b"
    type: short_answer
    text: |
      Give \textbf{two} examples of application software.

      1. \hrulefill
      2. \hrulefill
  - number: "4c"
    type: short_answer
    text: |
      Describe the difference between system software and application software.