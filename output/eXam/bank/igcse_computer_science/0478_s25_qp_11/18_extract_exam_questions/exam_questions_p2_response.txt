[thinking]
**My Analysis of the Exam Question Extraction Task**

Alright, let's break this down. My goal is to transform the content of this exam paper page into a clean YAML format, perfectly adhering to the specified schema. It's a pretty straightforward task, but precision is key.

First, I need to pinpoint the questions to be extracted. I've identified the specific questions that are relevant: "1", "1a", "1b", "1c", "1ci", "1cii", "2", and "2a".  I'll then analyze the text for each of those questions. I'll make sure to note the question type based on the content (multiple choice, calculation, short answer).

Now, the formatting is crucial. YAML is the target, so I'll be using block scalars (`|`) to handle multi-line text, ensuring no unnecessary double quotes. I'm also mindful of the specific instructions to remove subquestion prefixes like (a), (b), etc., and those pesky "Working space" headers and dotted lines. I need to get rid of any extraneous elements and only capture the core questions and options.

With a good understanding of the instructions, I'll assemble the YAML structure. Each question gets a `number`, `type`, and `text` field. Multiple-choice questions will include an `options` list with `letter` and `text` for each choice. I'll ensure the content is correctly placed under the corresponding keys.

Finally, I'll perform a thorough review.  I'll double-check that strings are not unnecessarily enclosed in double quotes and ensure the use of block scalars where required.  I'll verify that I haven't included any unwanted elements, such as standalone dotted lines or prefixes. The question types need to be correct and I also need to ensure that the order is correct. I have to make sure that the numbers align, all is stripped appropriately and everything is presented correctly!



[/thinking]

questions:
  - number: "1"
    type: short_answer
    text: |
      The size of a file can be measured using different data storage units.
  - number: "1a"
    type: multiple_choice
    text: |
      Tick ($\checkmark$) \textbf{one} box to show the smallest data storage unit.
    options:
      - letter: A
        text: |
          byte
      - letter: B
        text: |
          gibibyte (GiB)
      - letter: C
        text: |
          kibibyte (KiB)
      - letter: D
        text: |
          nibble
  - number: "1b"
    type: calculation
    text: |
      State how many tebibytes (TiB) are equal to 2 pebibytes (PiB).
  - number: "1c"
    type: short_answer
    text: |
      The size of a file needs to be reduced.
  - number: "1ci"
    type: short_answer
    text: |
      Give the name of the process that is used to reduce the size of a file.
  - number: "1cii"
    type: short_answer
    text: |
      One reason the size of a file may need reducing is so that it has a shorter transmission time.

      Give \textbf{one} other reason why the size of a file may need reducing.
  - number: "2"
    type: short_answer
    text: |
      A clothes store has a ticket system to allow customers to speak to a personal shopper.

      The ticket displays a ticket number in hexadecimal, for example 11B.
  - number: "2a"
    type: short_answer
    text: |
      The ticket system has an output device that creates a paper ticket.

      Identify an appropriate output device to produce the paper ticket.