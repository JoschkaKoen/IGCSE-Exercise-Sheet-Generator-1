[thinking]
**My Thought Process: Extracting and Formatting Exam Questions**

Okay, here's what I'm thinking. I need to take the content of page 8 of this exam paper and distill it into a structured YAML format. The goal is to isolate and represent each question and its components. I need to make sure the structure is correct, and I'm using the exact keys and structure as specified, matching the provided stub perfectly.

First, I identify the target questions: "5", "5a", "5ai", "5aii", "5b", and "5bi". I read through each section. I see that there are short answer and multiple choice questions.

Now I'll methodically extract the information and convert it to the required YAML structure. For each question, I create the YAML block. The `number` field will represent the question label (e.g., "5", "5a", etc.). The `type` will be either `short_answer` or `multiple_choice`, as appropriate. The `text` field will contain the question's actual text. To handle any multi-line question text or formatting within the text, I'll use the `|` block scalar in YAML.

As I work, I need to keep the constraints in mind. I need to make sure I don't use double quotes within the `text` fields; the block scalar (`|`) avoids that issue. I also need to ensure that there are no `\includegraphics` commands.

For question `5aii`, I see numbered slots for the answers. These are represented using `1. \hrulefill`, `2. \hrulefill`, and `3. \hrulefill`. This needs to be precisely rendered.

For multiple-choice questions like `5bi`, I'll include an `options` section, where each option has a `letter` and a `text`.

Finally, before finalizing each question, I need to check one final thing. I double check the text of each question to make sure that none of the subquestion prefixes like `(a)`, `(b)`, `(i)`, or `(ii)` make it into the final output. Wait, I realize that I'll need to remember that the text for `5a`, for example is "The computer has..." and the format must have the same text. The text extraction for the questions matches perfectly to my current method. Now it's just a matter of assembling the blocks, and I'll be done. I'll make sure to use `\checkmark` for the tick symbol as it matches standard LaTeX and the requested format. I think I'm good to go!



[/thinking]

questions:
  - number: "5"
    type: short_answer
    text: |
      A computer is connected to a network.
  - number: "5a"
    type: short_answer
    text: |
      The computer has a network interface card (NIC) that has a media access control (MAC) address.
  - number: "5ai"
    type: short_answer
    text: |
      One purpose of the NIC is to provide the computer with a MAC address.

      Describe the other purposes of the NIC.
  - number: "5aii"
    type: short_answer
    text: |
      Identify \textbf{three} characteristics of a MAC address.

      1. \hrulefill
      2. \hrulefill
      3. \hrulefill
  - number: "5b"
    type: short_answer
    text: |
      The network allocates an internet protocol (IP) address to the computer.
  - number: "5bi"
    type: multiple_choice
    text: |
      Tick (\checkmark) \textbf{one} box to identify a valid IPv4 address.
    options:
      - letter: A
        text: |
          110:255:2:1
      - letter: B
        text: |
          1.30.2FF.A9
      - letter: C
        text: |
          3.162.74.3
      - letter: D
        text: |
          8.0.257.6.8