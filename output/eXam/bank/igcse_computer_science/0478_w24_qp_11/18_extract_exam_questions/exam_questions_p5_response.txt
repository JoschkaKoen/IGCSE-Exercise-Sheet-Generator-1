[thinking]
**Transcribing the Questions**

Okay, the user wants me to transcribe these questions into a formatted structure. It seems straightforward enough. Let's break it down question by question.

First, question `3c`: It's a short, descriptive statement: "The character 'y' is represented by the binary ASCII number 01111001." I'll format this as a YAML entry.

Next, question `3ci`: This one's a calculation: "Convert the binary ASCII number to denary." Again, a YAML entry is the obvious choice.

Then, `3cii`: Another calculation, asking for the hexadecimal equivalent: "Convert the binary ASCII number to hexadecimal." This follows the same format as the previous calculation.

Finally, `3ciii`: This involves a logical right shift: "A logical right shift of two places is performed on the binary ASCII number 01111001. Give the binary number after the logical right shift of **two** places is performed." I'll format this into a calculation YAML structure, keeping the bold formatting for the shift amount as it's provided.

I've made sure to omit any unnecessary elements such as answer lines or working space headers as I've been instructed. Should be good to go.



[/thinking]

questions:
  - number: "3c"
    type: short_answer
    text: |
      The character 'y' is represented by the binary ASCII number 01111001.
  - number: "3ci"
    type: calculation
    text: |
      Convert the binary ASCII number to denary.
  - number: "3cii"
    type: calculation
    text: |
      Convert the binary ASCII number to hexadecimal.
  - number: "3ciii"
    type: calculation
    text: |
      A logical right shift of two places is performed on the binary ASCII number 01111001.

      Give the binary number after the logical right shift of \textbf{two} places is performed.