[thinking]
**My Analysis of the Exam Question Extraction Task**

Alright, let's break this down. My objective is clear: I need to extract three questions, specifically "7", "7a", and "7b", from a given exam paper page (page 12, to be precise). The output has to be in YAML format, a structure I'm intimately familiar with. The format requires a `number` for the question itself, a `type` that describes what kind of question it is (short answer, long answer, multiple choice, etc.), and the actual `text` of the question. Multiple-choice questions would also need an `options` section, but I'll hold off on that until I encounter one.

The first step is to analyze the content of the page itself. I'm focusing on those three key questions.

*   **Question 7:** It seems to set the scene, providing context to the broader topic of data transmission and introducing an error scenario. This will likely be formatted as a short answer context question.

*   **Question 7a:** This is a straightforward long-answer question, asking for a description of how transmission errors can occur. I'll need to pay attention to the mark allocation to understand the level of detail required.

*   **Question 7b:** This one is a fill-in-the-blanks short answer type question focused on error detection and correction methods. I note the provided word list is a critical constraint. I need to be careful to structure that list properly with appropriate markup. I'll need to use the provided terms, *only* the provided terms, and *each* term only once. This is key to getting this right. I need to format the blanks correctly using `\underline{\hspace{1.5em}}`.

Now, I'll structure each question in YAML, keeping in mind the formatting requirements.

*   **Question 7:** It's just context so I'll render a simple short answer.

*   **Question 7a:** Straightforward YAML format for a long answer.

*   **Question 7b:** This one needs the most care to ensure the list is formatted correctly, and the blanks are properly rendered. The itemize tag has been added to improve clarity. I must ensure the block scalar syntax `|` is used for all text fields to avoid escaping double quotes. And finally, I must remove any subquestion prefixes such as "(a)" and "(b)" to keep the format concise.

Finally, a quick review, adhering to the output constraints. I've got to make sure there are no double quotes around non-empty strings. Everything will be wrapped using the block scalar (`|`) for the text fields. There are no standalone dotted lines or escape characters. The inline blanks are styled correctly using `\underline{\hspace{1.5em}}`, and the word list uses itemize correctly. I believe this provides a well-structured and accurate representation of the exam questions.



[/thinking]

questions:
  - number: "7"
    type: short_answer
    text: |
      Data is sent from an employee's computer to a server. The data is correct when it is sent, but an error is detected when the data is received.
  - number: "7a"
    type: long_answer
    text: |
      Describe how an error can occur in the data during transmission.
  - number: "7b"
    type: short_answer
    text: |
      Methods of error detection and correction can be added to make sure that errors are identified after data is transmitted.

      Complete the statements about methods of error detection and correction.

      Use terms from the list. Not all terms need to be used. You should only use each term once.

      \begin{itemize}
      \item acknowledgement
      \item automatic repeat query (ARQ)
      \item binary
      \item bit
      \item byte
      \item calculation
      \item check digit
      \item checksum
      \item correct
      \item echo check
      \item error
      \item even
      \item incorrect
      \item negative
      \item number
      \item odd
      \item positive
      \item timeout
      \end{itemize}

      A parity check is set to be \underline{\hspace{1.5em}} or \underline{\hspace{1.5em}}. A parity \underline{\hspace{1.5em}} is added to each byte before transmission to make it match the set parity.

      An \underline{\hspace{1.5em}} involves comparing the data sent to the data received back from the receiving device.

      An \underline{\hspace{1.5em}} uses acknowledgement and \underline{\hspace{1.5em}}. The acknowledgement system can be \underline{\hspace{1.5em}} or \underline{\hspace{1.5em}}.