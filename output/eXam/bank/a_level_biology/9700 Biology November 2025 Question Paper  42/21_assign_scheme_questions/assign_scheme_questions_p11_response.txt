[thinking]
The user wants me to identify the question numbers from the provided mark scheme page.

1.  **Analyze the image:**
    *   The image is a table with columns "Question", "Answer", and "Marks".
    *   I need to look at the "Question" column to find the question numbers.

2.  **Extract Question Numbers:**
    *   Row 1: "3(a)(i)" -> This corresponds to `3ai` in the allowed list format (removing parentheses). Wait, let's check the allowed list format carefully.
        *   Allowed list: "1", "1a", "1b", "1c", "1ci", "1cii", "2", "2a", "2b", "3", "3a", "3ai", "3aii", "3aiii", "3b", "3bi", "3bii", ...
        *   The text in the image is "3(a)(i)". This maps to `3ai`.
    *   Row 2: "3(a)(ii)" -> This corresponds to `3aii` in the allowed list format.

3.  **Verify against the allowed list:**
    *   `3ai` is in the list.
    *   `3aii` is in the list.

4.  **Format the output:**
    *   The output must be YAML.
    *   The keys should be under `questions`.
    *   The values should be strings.

    So, the result should be:
    ```yaml
    questions:
      - "3ai"
      - "3aii"
    ```

5.  **Final Check:**
    *   Are there any other questions? No.
    *   Is the formatting correct? Yes.
    *   Did I remove parentheses? Yes, "3(a)(i)" became "3ai". "3(a)(ii)" became "3aii".

    Let's double check the mapping.
    Image: "3(a)(i)" -> List item: "3ai". Correct.
    Image: "3(a)(ii)" -> List item: "3aii". Correct.

    The task asks for "question numbers whose marking criteria are visible".
    The first row has criteria for 3(a)(i).
    The second row has criteria
[/thinking]

```yaml
questions:
  - "3ai"
  - "3aii"
```