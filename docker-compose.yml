services:
  web:
    build: .
    restart: unless-stopped
    ports:
      - "80:8000"
    # default.env = committed defaults; .env = secrets (optional file; create when needed)
    env_file:
      - path: default.env
        required: true
      - path: .env
        required: false
