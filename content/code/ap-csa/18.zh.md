## 多态:一种类型,多种行为

- **多态**(polymorphism)的意思是"多种形态":一个父类类型,多种子类行为。
- 你可以把一个 `Dog` 或一个 `Cat` 放进一个 `Animal` 变量里,因为它们各自**是一种** `Animal`。
- 当你调用一个方法时,由**对象的真实类型**决定运行哪个版本。

---

## 动态分派

- 变量的类型(`Animal`)决定你**被允许**调用哪些方法。
- 但在运行时,由**实际的对象**(`Dog`)决定运行哪个**被重写**的版本。
- 这种运行时的选择叫作**动态分派**(dynamic dispatch) —— 它是多态的核心。

---

```java
public class Main {
    public static void main(String[] args) {
        Animal[] zoo = { new Dog(), new Cat(), new Dog() };
        for (Animal a : zoo) {
            System.out.println(a.speak());   // Woof, then Meow, then Woof
        }
    }
}

class Animal {
    public String speak() { return "some sound"; }
}
class Dog extends Animal {
    public String speak() { return "Woof"; }
}
class Cat extends Animal {
    public String speak() { return "Meow"; }
}
```

---

## 一个循环处理所有子类

- 上面的循环从不问"这是 Dog 还是 Cat?" —— 它只是调用 `speak()`。
- 每个对象用**自己的方式**回答。这让循环保持简短而通用。
- 以后再加一个全新的子类,**同一个循环**不用改就能处理它。

---

## `toString()`:每个对象都从 `Object` 继承它

- Java 里每个类其实都暗中继承自一个叫 `Object` 的类,它有一个 `toString()`。
- 重写 `toString()`,来说明你的对象作为文本时应该长什么样。
- 当你打印一个对象,或用 `+` 拼接它时,Java 会**自动**调用它。

---

```java
public class Main {
    public static void main(String[] args) {
        Object x = new Fraction(3, 4);
        System.out.println(x);              // 3/4   -- toString() runs
        System.out.println("ratio " + x);   // ratio 3/4
    }
}

class Fraction {
    private int numerator;
    private int denominator;
    public Fraction(int numerator, int denominator) {
        this.numerator = numerator;
        this.denominator = denominator;
    }
    public String toString() {
        return numerator + "/" + denominator;
    }
}
```

---

## 现在轮到你

- 用一个父类数组、加一个新子类,再重写 `toString()`。
- 一个隐藏的 **Harness**(检查程序)会检查每个对象运行的是正确的版本。
- 按 **Run** 编译,然后 **Check answer**。你的代码在服务器上运行,所以第一次运行也很快。
