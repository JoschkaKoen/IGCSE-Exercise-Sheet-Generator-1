# 选择列

`SELECT *` 返回**每一**列。通常你只想要其中几列。不用 `*`,而是列出你想要的列,用逗号隔开:

```sql
SELECT name, score FROM student;
```

结果就只有这些列,顺序和你写的一样。

---

# 用 AS 给列改名

你可以用 `AS` 给**结果中的**列改名。这不会改动表本身 —— 只改变显示的标题:

```sql
SELECT name AS pupil, score AS mark FROM student;
```

标题就变成了 `pupil` 和 `mark`。这对做清晰的报表很有用。
