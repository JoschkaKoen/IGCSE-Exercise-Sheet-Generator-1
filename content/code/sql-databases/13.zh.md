# 修改表的结构

表的设计不是一成不变的。`ALTER TABLE … ADD` 给已有的表添加一个新列:

```sql
ALTER TABLE product ADD weight DECIMAL(5, 2);
```

在你填入数据之前,已有的行在新列里都是 `NULL`。

---

# 删除一张表

`DROP TABLE` 删除整张表 —— 包括它的结构**和**所有行:

```sql
DROP TABLE product;
```

这无法撤销,所以要小心使用。删掉一张被其他表引用的表,可能破坏参照完整性。
